/**
 * Component to log users
 */

import React, {useState} from 'react';

const LoginForms = () => {

    const initialCredentials = {
        user: '',
        password: ''
    }

    const [credentials, setCredentials] = useState(initialCredentials);

    return (
        <div>
            
        </div>
    );
}

export default LoginForms;
